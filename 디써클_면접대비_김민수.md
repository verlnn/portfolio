# 디써클 1차 기술 면접 대비 — 김민수

> **포지션**: 디써클(D.Circle / RnDcircle) 백엔드 엔지니어
> **면접 형식**: 1차 비대면 기술 면접 (20분)
> **다음 단계**: 2차 대면 기술 면접 → 경영진 면접 → 최종

---

## 📌 면접 직전 마인드셋

1. **20분은 짧다** — 자기소개 + 핵심 질문 2~3개 + Q&A 정도
2. **모든 답변에 트레이드오프를 의식적으로 녹일 것** — "이 선택의 이점은 X, 대신 Y를 포기했고, Z로 보완"
3. **모르는 건 솔직하게** — "직접 경험은 없지만 개념은 이해하고, 이렇게 학습 중입니다"
4. **운영 관점 강조** — 디써클은 "운영 가능성"을 중시하는 채용공고
5. **회사·서비스 사용 경험 한 줄 준비** — rndcircle.io 직접 가입해보기

---

## 🎯 회사 핵심 정보 (면접 답변 시 자연스럽게 인용)

| 항목 | 내용 |
|---|---|
| 사명 | 주식회사 디써클 (D.Circle), 서비스명 **알앤디써클(RnDcircle)** |
| 설립 | 2024년 1월 (앤틀러 코리아 3기 출신) |
| 공동대표 | 이윤구, 장재우 |
| 사업 | AI 기반 산학연 R&D 파트너 매칭 SaaS 플랫폼 |
| 데이터 규모 | 전국 **3만+ 연구실 / 20만+ 기업** 데이터 |
| 주요 기술 | LLM 기반 매칭, 논문 콘텐츠화, 비정형 데이터 처리 |
| 성과 | 100+ 기업, 250+ 매칭 성사, 매칭 시간 8주→1~2주 단축 |
| 미션 | "세상에 필요한 기술의 출현 및 상용화를 가속화" |
| 인프라 | GCP, Kotlin/Spring Boot, RabbitMQ, Elasticsearch, Redis, PostgreSQL, MongoDB |

---

## 🗣️ 자기소개 (60~90초)

> 안녕하세요, 4년 4개월간 플랜잇스퀘어에서 백엔드 개발을 해온 김민수입니다.
>
> 서울대학교병원을 포함한 12개 다기관 의료 환경에서 최대 60억 건 규모의 임상 데이터를 다루는 시스템을 운영했고, 단순 기능 구현보다는 **운영 환경에서 안정적으로 동작하는 시스템을 만드는 것**에 강점이 있습니다.
>
> 특히 **Elasticsearch 기반 검색 시스템과 자체 적재 모듈 개발**, **레거시 AI 솔루션을 자체 엔진으로 대체해 정확도와 운영 통제력을 확보한 경험**, **Spring Batch 기반 대용량 배치 파이프라인 운영 경험**을 갖고 있습니다.
>
> 디써클이 다루는 비정형 연구 데이터와 대규모 매칭 문제는 제가 그동안 풀어온 문제와 본질적으로 닿아 있다고 느껴서, 제 강점을 가장 잘 살릴 수 있는 곳이라 판단해 지원했습니다. 잘 부탁드립니다.

### 자기소개 시 주의사항
- "병역특례", "특성화고 졸업" 같은 정보는 묻지 않으면 먼저 꺼내지 않기
- 60~90초 안에 끝내기 (길면 면접관이 끊음)
- 마지막에 "잘 부탁드립니다" 같은 정중한 마무리

---

## 💼 핵심 답변 카드 (3대 트레이드오프 사례)

> 디써클 채용공고에 명시된 **"구현했던 시스템 디자인의 트레이드오프 설명"** 은 거의 100% 출제. 미리 3개를 준비하고 면접 흐름에 따라 골라서 사용.

---

### 🥇 카드 1 — IBM Watson Discovery → 자체 정규식 엔진(ExtractEx)

**언제 쓰는가**: "트레이드오프 경험", "AI 활용 경험", "리드한 프로젝트", "잘못된 의사결정 개선" 모두 가능 — 만능 카드

#### 답변 구조 (STAR + 트레이드오프)

```
[상황 - Situation]
서울대병원 병리 데이터(비정형 텍스트)를 구조화하는 시스템을
초기엔 IBM Watson Discovery 기반으로 운영하고 있었습니다.

[문제 - Task]
운영하면서 세 가지 한계가 명확해졌습니다:
1. 적재 처리 시간이 길어 대용량 데이터 처리 병목
2. 외부 API 의존으로 장애 시 운영 통제력 부족
3. 도메인 특화 케이스에서 Hit율이 기대보다 낮음

[고려한 옵션]
- Option A: Watson Discovery 유지 + 후처리 보강
- Option B: 정규식 기반 자체 엔진으로 전면 대체
- Option C: 다른 NLP 솔루션 도입

[행동 - Action]
Option B를 선택했습니다. 도메인이 충분히 좁고
패턴이 반복적이라 정규식 기반이 더 적합하다고 판단했습니다.

ExtractEx라는 자체 파싱 엔진을 설계하고
CompletableFuture 기반 병렬 처리를 적용했습니다.

[결과 - Result]
- Hit율 97% 달성
- 외부 의존성 제거로 운영 통제력 확보
- 적재 시간 대폭 단축

[트레이드오프 인식]
얻은 것: 정확도, 운영 통제력, 성능
포기한 것: AI 기반 학습형 일반화 능력
       (새 패턴이 나타나면 정규식 추가 필요)

[완화 방법]
정규식 패턴을 코드 하드코딩이 아닌 외부 관리 구조로 분리해서,
새 케이스가 들어와도 재배포 없이 패턴 추가 가능하게 했습니다.

[회고]
이 결정은 "도메인이 좁다"는 전제 위에서 옳았습니다.
도메인이 매우 넓었다면 LLM 기반이 더 적절했을 것이고,
지금이라면 LLM + 정규식 하이브리드도 고려할 만하다고 봅니다.
```

#### 🔥 예상 꼬리 질문

| 질문 | 답변 핵심 |
|---|---|
| 정확도(Hit율)를 어떻게 측정했나요? | 정답 데이터셋(병리과 검증 데이터)과 비교한 Precision/Recall |
| CompletableFuture 왜 썼어요? Stream parallel은? | I/O 바운드 작업이라 thread pool을 명시적으로 제어할 수 있는 CompletableFuture가 적합. parallelStream은 ForkJoinPool 공유라 제어 어려움 |
| 정규식이 복잡해지면 유지보수 어렵지 않나요? | 패턴별로 모듈 분리, 단위 테스트 + 정답 데이터 회귀 테스트로 안전성 확보 |
| 지금 다시 한다면? | LLM(예: GPT-4o-mini) + 정규식 하이브리드 — 정규식으로 1차 빠르게, LLM으로 fallback |

---

### 🥈 카드 2 — Logstash → 자체 적재 모듈(ELM)

**언제 쓰는가**: "Elasticsearch 경험", "운영 안정성", "기술 의사결정"

```
[상황]
Elasticsearch에 대용량 의료 데이터를 적재하는 데
초기엔 Logstash를 썼습니다.

[문제]
- 적재 실패 시 어디서 끊겼는지 추적 어려움
- 적재 상태 관리·재시도 로직 세밀한 제어 불가
- 운영 중 장애 발생 시 빠른 진단 안 됨

[행동]
Bulk Request API 기반 자체 적재 모듈(ELM)을 개발했습니다.

[결과]
- 적재 상태 추적 (어디서 실패했는지 명확)
- 오류 케이스별 재시도 정책 적용
- 운영 가시성 확보

[트레이드오프]
얻은 것: 운영 통제력, 모니터링 가능성
포기한 것: Logstash의 풍부한 생태계, 무료 유지보수
       (이제 우리가 직접 유지보수해야 함)

[완화]
Bulk 사이즈, 재시도 정책 등을 설정 기반으로 만들어
코드 변경 없이 튜닝 가능하게 함
```

#### 🔥 예상 꼬리 질문

| 질문 | 답변 핵심 |
|---|---|
| Bulk API의 장점? | 네트워크 라운드트립 감소, 처리량 향상. 단, 한 번에 너무 많이 보내면 메모리 압박 |
| Bulk 사이즈 어떻게 정했어요? | 처음엔 1,000건으로 시작 → 실측해서 5,000~10,000건 사이에서 throughput 최적 |
| Refresh interval은 어떻게? | 적재 중에는 -1로 비활성화 → 끝난 후 1s로 복구 (검색 가능 시점 vs 적재 속도 트레이드오프) |
| 실패 시 어떻게 재시도? | 일시적 오류(네트워크) → 지수 백오프 재시도 / 영구적 오류(스키마 불일치) → DLQ로 격리 |

---

### 🥉 카드 3 — ResearchIDP (자체 SSO 서버) — 한 사이클 경험 카드

**언제 쓰는가**: "리드한 프로젝트", "설계 → 운영 한 사이클", "아키텍처 의사결정"

```
[배경]
ResearchEx, DataHub 등 여러 서비스가 각자 인증을 처리해서
사용자는 서비스마다 따로 로그인해야 했고
인증 로직이 서비스마다 중복 구현됐습니다.

[설계 결정]
대안 비교:
- A. 서비스 간 토큰 공유 → 토큰 관리 복잡도 증가로 기각
- B. OAuth 2.0 풀 구현 → 내부 시스템엔 과한 스펙
- C. 중앙 IDP + OAuth 유사 인증 흐름 + 단일 세션 ← 선택

[구현]
- Spring Security 기반 IDP 서버
- 세션 저장소: PostgreSQL (Redis도 검토했지만 운영 단순화 우선)
- SDK는 Spring Boot Starter 패턴으로 캡슐화

[성과]
- 분산 인증 → 중앙화로 중복 제거
- 신규 서비스 연계 시 SDK 한 줄로 통합
- 사용자 로그인 편의성 향상

[트레이드오프]
얻은 것: 중앙 집중식 관리, 신규 서비스 통합 비용 ↓
포기한 것: IDP 단일 장애점(SPoF) 리스크
       — 이건 운영 환경에서 모니터링 강화로 보완

[배운 점]
처음부터 끝까지 책임진 시스템을 운영하면서
"설계 시점의 결정이 운영 비용으로 돌아온다"는 걸 체감
```

#### 🔥 예상 꼬리 질문

| 질문 | 답변 핵심 |
|---|---|
| JWT 안 쓰고 세션 기반? | 내부 서비스 통신, 즉시 로그아웃 강제 가능, 세션 저장소를 통한 중앙 제어 우선 |
| Redis 안 쓰고 PostgreSQL? | 운영 환경 단순화 우선. 트래픽 규모상 PG로 충분했고, 새 컴포넌트 추가는 신중하게 |
| OAuth 2.0과 어떻게 다른가? | Authorization Code Grant 흐름은 차용했지만, 외부 클라이언트가 없어 PKCE 등은 생략. Refresh Token 정책도 단순화 |
| SPoF는 어떻게? | 운영 환경에선 active-passive 구성 + 헬스체크. 가능한 다음 개선은 multi-instance + 세션 저장소 분리 |

---

## 🎯 자격요건별 답변 카드

### ✅ 자격요건: "설계 → 개발 → 배포 → 운영 한 사이클"
- **메인 카드**: ResearchIDP (위 카드 3)
- **보조 카드**: 멍냥마당 (개인 프로젝트, Docker Compose + EC2 + GitHub Actions CI/CD 풀스택 구축)

### ✅ 자격요건: "자료구조와 알고리즘 효율성"

#### 메인 사례: 룩업 조회 O(N×M) → O(N) 개선

```
[문제 인식]
연구자가 데이터셋을 구성할 때 룩업 조회(코드→의미)가 필요했는데,
브라우저 프리징 발생.

원인:
- 외부 데이터 N개 행에 대해
- 룩업 테이블 M개를 매번 List.contains()로 순회
- O(N×M) 시간복잡도

[해결]
검색 대상을 List → HashSet으로 변환.
- HashSet의 contains()는 평균 O(1) — 해시 테이블 기반
- N번 조회 × O(1) = O(N)으로 개선

[트레이드오프]
메모리는 약간 더 쓰지만, N과 M이 모두 큰 환경에선
시간 비용이 압도적으로 더 컸음 → 명확한 선택
```

#### 🔥 꼬리 질문 대비 (반드시 외울 것)

| 질문 | 답변 |
|---|---|
| **HashSet 내부 구현은?** | HashMap의 key만 사용. value는 dummy 객체. 해시 테이블 기반 |
| **해시 충돌 시 어떻게?** | Java 8 이전: chaining (LinkedList). Java 8+: chain 길이 8 초과 시 Red-Black Tree로 변환 (treeify) |
| **HashSet vs TreeSet?** | HashSet O(1) 평균, 정렬 X / TreeSet O(log N), 정렬 O |
| **LinkedHashSet은?** | 삽입 순서 유지, 내부적으로 HashMap + 이중 연결 리스트 |
| **HashMap의 capacity와 load factor?** | 기본 capacity 16, load factor 0.75. 초과 시 2배로 resize |
| **equals와 hashCode 계약?** | equals가 true면 hashCode도 같아야 함. 반대는 성립 X. 안 지키면 HashSet/HashMap 동작 깨짐 |

---

### ✅ 자격요건: "개발 패러다임, 아키텍처, 디자인 패턴, CI/CD, 테스팅 깊이"

#### A. 헥사고날 아키텍처 (RidEx에 적용)

```
[배경]
RidEx(비식별화 시스템)는 다중 DB(PostgreSQL, Oracle, Vertica)를
다뤘고, Spring Batch 기반 처리도 필요했음.

[문제]
- 도메인 로직(비식별화 규칙)이 데이터 접근 코드와 섞임
- DB 늘 때마다 도메인 코드도 수정
- 테스트하려면 실제 DB 필요 → 단위 테스트 어려움

[적용]
- Domain (비식별화 규칙) ← 순수 비즈니스 로직
- Port (인터페이스) ← 외부 세계와 계약
- Adapter (구현체) ← PostgreSQL, Oracle, Vertica 어댑터

[얻은 것]
1. 도메인 로직을 DB 없이 단위 테스트 가능
2. 새 DB 추가 시 어댑터만 추가, 도메인 변경 X
3. Spring Batch와 도메인 분리 → 배치 외 호출도 가능

[트레이드오프]
- 코드량 증가 (인터페이스, DTO 매핑)
- 작은 프로젝트엔 오버엔지니어링 → RidEx에만 적용
```

#### 🔥 꼬리 질문

| 질문 | 답변 |
|---|---|
| Layered, Clean, Hexagonal 차이? | Layered: 위→아래 의존. Clean/Hexagonal: 도메인 중심, 의존성 역전. Hexagonal은 Port/Adapter 명시적 분리 |
| Port와 Adapter 어떻게 분리? | Port = 인터페이스 (도메인이 정의). Adapter = 구현체 (인프라 계층) |
| DDD와의 관계? | Hexagonal은 아키텍처 패턴, DDD는 설계 방법론. 같이 쓸 수 있음 (Aggregate, Bounded Context를 Hexagonal로 구현) |

---

#### B. Spring Batch (한화생명 IMD, RidEx)

```
[Chunk 처리 이해]
Reader → Processor → Writer를 chunk 단위로 묶어
트랜잭션 단위로 처리.

chunk 사이즈 = 메모리 vs 처리 속도 트레이드오프:
- 너무 크면 메모리 압박 + 실패 시 재처리 비용 ↑
- 너무 작으면 트랜잭션 오버헤드 ↑
- 보통 1,000~10,000으로 시작 후 실측 조정

[재시작 가능성]
JobRepository에 실행 상태 저장 → 실패 시 마지막 성공 chunk
다음부터 재시작 가능. 운영에서 진짜 중요한 가치.

[운영 고려]
- 실패 분류: 일시적(재시도) vs 영구적(Skip + 로그)
- Skip 정책, Retry 정책 명시 설정
- 메트릭: 처리 건수, 실패 건수, 처리 시간
```

#### 🔥 꼬리 질문

| 질문 | 답변 |
|---|---|
| Tasklet vs Chunk? | Tasklet: 단일 작업 (단순 SQL 실행 등). Chunk: read-process-write 루프, 트랜잭션 단위 |
| JobParameters 왜 필요? | 같은 Job을 여러 번 실행할 때 인스턴스 구분. 보통 timestamp나 날짜로 |
| Step 간 데이터 공유는? | ExecutionContext (Step Context, Job Context) |
| Partition 경험 있어요? | 직접 운영 경험 없지만 개념 이해. 단일 Job을 여러 worker로 분할. 디써클 데이터 규모면 도입 자연스러울 듯 |

---

#### C. 테스팅 전략

```
[원칙]
1. 단위 테스트: 도메인 로직 — 헥사고날 적용 시 가장 효과적
2. 통합 테스트: API 레벨, 실제 DB 함께 (Testcontainers)
3. 운영 검증: 핵심 시나리오는 배포 후 스모크 테스트

[Mockito 활용]
외부 의존성(DB, 외부 API)은 모킹 → 도메인 로직에 집중

[테스터블 코드 원칙]
- 의존성 주입으로 외부 의존을 인터페이스로
- 정적 메서드, 싱글톤 남용 회피
- 부수 효과(side effect) 격리

[솔직한 갭]
TDD 수준은 못 갔고, 핵심 기능 후 테스트 추가 방식.
디써클이 강조하는 "테스터블한 코드 + 효과적 Test Suite"는
제가 더 발전시키고 싶은 영역.
```

#### 🔥 꼬리 질문

| 질문 | 답변 |
|---|---|
| @MockBean vs @Mock? | @MockBean: Spring 컨텍스트에 등록 (통합 테스트). @Mock: 단순 Mockito (단위 테스트) |
| 테스트 더블 종류? | Dummy, Stub, Spy, Mock, Fake — Martin Fowler 분류 |
| 통합 테스트의 DB 처리? | Testcontainers (실제 DB 컨테이너), H2 (인메모리), @Transactional 롤백 |
| @SpringBootTest 무겁지 않아요? | 무거움. 슬라이스 테스트(@DataJpaTest, @WebMvcTest)로 필요한 부분만 띄우는 게 좋음 |

---

#### D. CI/CD (멍냥마당 개인 프로젝트)

```
[파이프라인]
GitHub Push → GitHub Actions
  → 1. 테스트 실행
  → 2. Docker 이미지 빌드
  → 3. Docker Hub 푸시
  → 4. AWS EC2에서 docker pull & 재시작

[배운 점]
- 자동화로 작은 변경도 부담 없이 푸시
- 환경별 분리(dev/prod) 필요성 체감
- 시크릿은 GitHub Secrets로

[솔직한 한계]
이미지 태그를 명시 관리하지 않아 롤백 어려웠음.
다음엔 git SHA 기반 태깅으로 개선할 계획.
```

---

## 🌟 우대사항별 답변 카드

### 우대사항 1: "테스터블한 코드 + 다양한 아키텍처 유지보수"

> 테스트 가능한 코드를 만들기 위해 의존성 역전과 인터페이스 분리를 의식적으로 적용해왔습니다. 가장 명확한 사례는 **RidEx에서 헥사고날 아키텍처를 도입한 것**이었고, 그 외 일반 프로젝트에서도 Service/Repository 인터페이스 분리, 외부 호출 추상화를 통해 단위 테스트 가능성을 확보하려 했습니다. **MSA, DDD, Hexagonal**을 실무에서 다뤄봤습니다.

### 우대사항 2: "안정적이고 확장 가능한 배치 데이터 파이프라인"

> 한화생명 IMD에서 일일 배치로 금융 데이터를 적재했고, Spring Batch의 chunk 처리, JobRepository 기반 재시작, Skip/Retry 정책을 직접 적용했습니다. RidEx에서는 비식별화 처리에도 Spring Batch를 적용했고요. 디써클이 운영하는 **Dagster나 n8n**은 직접 써본 적 없지만, Job/Step 추상화의 본질이 비슷해 빠르게 적응할 수 있을 것이라 봅니다.

### 우대사항 3: "ML, LLM 서비스 적용 경험"

> IBM Watson Discovery 기반 비정형 의료 텍스트 파싱 시스템을 도입·운영하며 AI 솔루션의 적용 한계를 실전에서 판단해본 경험이 있습니다. 사이드 프로젝트 Food Wallet에서는 **GPT-4o-mini API와 OCR을 결합한 식품 정보 분석 파이프라인**을 직접 구현했습니다. 프롬프트 설계, JSON 응답 강제, 비용·속도 트레이드오프를 직접 다뤘습니다. **LangChain이나 RAG는 학습 중**입니다.

### 우대사항 4: "대규모 웹 크롤러"

> 직접 운영 경험은 없습니다. 다만 외부 기관(KBN, KOTRY) 연계를 위한 **Shuttle 시스템**에서 주기적 데이터 송수신 자동화와 오류 처리를 다뤘던 경험이 있고, 이게 크롤러 운영의 일부 영역과 겹친다고 봅니다.

### 우대사항 5: "오픈소스 기여"

> 기여 경험은 아직 없습니다. 다만 사용하는 라이브러리의 소스코드를 자주 읽어보는 편이고, 향후 기여 가능한 영역을 찾고 있습니다.

### 우대사항 6: "LLM 활용 개발 (Cursor, Claude Code 등)"

> Cursor를 메인 IDE로 사용 중이고, NotebookLM으로 기술 문서 분석, V0로 UI 프로토타이핑, ChatGPT/Claude로 의사결정 검토를 일상적으로 활용합니다. AI 도구로 단순 작업 시간을 줄이고 설계와 검증에 시간을 더 쓰는 방식으로 일하고 있습니다.

---

## 🚧 디써클 갭 영역 답변

### Kotlin 미경험

> Kotlin 실무 경험은 없습니다. 다만 Java 4년+ 경험이 있고, Kotlin은 JVM 위에서 동작하는 언어라 학습 곡선이 가파르지 않다고 봅니다. 현재 **null safety, data class, when expression, scope functions, coroutine 기초**를 학습 중이고, Spring Boot + Kotlin 토이 프로젝트로 직접 만져보고 있습니다. 입사 시점엔 실무 투입 가능한 수준으로 끌어올릴 계획입니다.

#### 면접 전 외울 Kotlin 핵심 개념

| 개념 | 핵심 |
|---|---|
| **null safety** | `String?` (nullable) vs `String` (non-null). `?.`, `?:`, `!!` 연산자 |
| **data class** | equals, hashCode, toString, copy 자동 생성 |
| **val vs var** | val = immutable (final), var = mutable |
| **when** | switch의 강화판, expression으로 사용 가능 |
| **scope functions** | let, run, with, apply, also — 객체 컨텍스트 처리 |
| **extension function** | 기존 클래스에 메서드 추가 가능 |
| **coroutine** | 경량 스레드, suspend 함수, 비동기 처리 |

### RabbitMQ vs Kafka

> Kafka는 사이드 프로젝트(HeavenlyCoupon)에서 쿠폰 발행 시스템에 써봤습니다. RabbitMQ는 학습 단계입니다. 두 메시지 큐의 차이는 **Kafka는 로그 기반 분산 스트리밍 플랫폼으로 높은 처리량과 재처리에 강하고, RabbitMQ는 AMQP 기반 메시지 브로커로 복잡한 라우팅과 즉시성에 강하다**는 점으로 이해하고 있습니다.

#### 비교표

| 항목 | RabbitMQ | Kafka |
|---|---|---|
| 모델 | AMQP 메시지 브로커 | 분산 로그 스트리밍 |
| 메시지 보존 | 소비 후 삭제 (기본) | 보존 기간만큼 유지 (재처리 가능) |
| 라우팅 | 풍부 (Direct, Topic, Fanout, Headers) | Topic 기반 단순 |
| 처리량 | 보통 (수만 msg/s) | 매우 높음 (수십만~수백만 msg/s) |
| 사용 사례 | 작업 분배, RPC, 복잡한 라우팅 | 이벤트 소싱, 로그 집계, 스트리밍 |

### Redis 활용 미숙

> Redis는 사이드 프로젝트(HeavenlyCoupon)에서 쿠폰 재고 관리에 써봤습니다. 자료구조별 용도(String, Hash, List, Set, Sorted Set)와 TTL, atomic 연산은 이해하고 있고, 캐시 전략(Cache-Aside, Write-Through, Write-Behind)도 학습했습니다. 디써클 환경에서 더 깊이 활용할 수 있게 학습 중입니다.

### Dagster / n8n

> 둘 다 직접 사용 경험은 없습니다. 다만 데이터 파이프라인 추상화(DAG, Asset, Op, Job)는 Spring Batch의 Job/Step 모델과 본질적으로 비슷해서, 개념 적응은 빠를 것이라 봅니다.

---

## ❓ 회사가 명시한 "이력서에 포함하면 좋은 내용" 답변

### Q. 본인이 직접 리드/주도했던 프로젝트
👉 **ResearchIDP (자체 SSO 서버)** — 카드 3 사용

### Q. 잘못된 기술적 의사결정 → 개선

```
초기에 룩업 테이블을 코드에 하드코딩하는 방식으로 시작했는데,
운영하면서 룩업이 바뀔 때마다 재배포가 필요하다는 게 큰 부담이 됐습니다.

이건 명백히 처음부터 DB 기반으로 갔어야 했던 결정이었고,
나중에 룩업 자동 빌드 시스템을 구축해
재배포 없이 운영 가능한 구조로 바꿨습니다.

교훈은 "초기에 빠른 구현을 위한 단축이 운영 비용으로 돌아온다"는 것입니다.
지금은 새 시스템을 설계할 때 항상
"이 결정이 운영 단계에서 어떻게 작동할지"를 먼저 고민합니다.
```

### Q. 아직 개선 못한 의사결정 / 더 나은 대안

```
의료 도메인 특성상 보안 요구가 강해서, 현재 시스템 일부가
모놀리식으로 묶여 있습니다. 헥사고날 아키텍처 적용으로
개선을 시작했지만, 본격적인 MSA 분리까지는 못 갔습니다.

다시 설계한다면 인증/검색/적재를 처음부터 독립 서비스로 분리하고,
메시지 큐(RabbitMQ나 Kafka) 기반 비동기 통신을 도입해
장애 격리와 확장성을 확보했을 것 같습니다.
```
*(✨ 디써클이 쓰는 RabbitMQ를 자연스럽게 언급)*

### Q. 새로운 기술 빠르게 검증한 경험

```
Elasticsearch를 처음 도입할 때, 기존 RDB 기반 검색의 한계
(Freetext 검색의 형태소 처리, 검색 정확도)를 극복하기 위해
2주간 PoC를 진행했습니다.

샘플 데이터로 RDB 풀텍스트 vs Elasticsearch의 정확도/성능을
직접 비교했고, 결과를 토대로 도입을 결정했습니다.

이후 Logstash의 한계를 발견했을 때도 비슷하게
자체 ELM 모듈 PoC를 빠르게 만들어 검증 후 본격 적용했습니다.
```

### Q. 어려운 비즈니스 문제를 다양한 방법으로 해결

```
방화벽 설정 오류로 전체 개발 환경의 DB 접근이 차단되는
장애가 발생했습니다. 코드 변경으로는 해결할 수 없는 문제였습니다.

코드 외 접근으로 Nginx Reverse Proxy + Stream 모듈을 활용한
DB 우회 구조를 설계해, 물리적 환경 변경 없이 개발 환경을 복구했습니다.

이 경험으로 "문제 해결은 반드시 코드일 필요가 없다"는 것을 배웠고,
인프라/네트워크 레벨까지 시야를 넓히게 됐습니다.
```

### Q. 시스템 디자인 트레이드오프
👉 **카드 1 (Watson → ExtractEx)** 이 메인, 카드 2/3은 보조

---

## 🤔 일반 기술 질문 대비 (가벼운 질문들)

### Elasticsearch 관련

| 질문 | 답변 |
|---|---|
| 왜 도입했나? | Freetext(병리 리포트) 검색에서 RDB 풀텍스트는 형태소 분석 한계, 정확도/속도 모두 부족 |
| 색인(index)이란? | 검색을 위한 역색인(inverted index) 구조. 단어 → 문서 ID 매핑 |
| Shard와 Replica? | Shard: 데이터 분할 (수평 확장). Replica: 복제본 (가용성 + 읽기 분산) |
| Bulk API 장점? | 네트워크 라운드트립 감소, 처리량 향상 |
| Mapping이란? | 필드 타입 정의 (text, keyword, date 등). text는 분석됨, keyword는 정확 일치 |

### Nginx Reverse Proxy

| 질문 | 답변 |
|---|---|
| 왜 두는가? | SSL 종단, 로드밸런싱, 정적 파일 처리, WAS 보호, 헤더 가공 |
| Web-WAS 분리 이유? | 책임 분리(Web=정적/프록시, WAS=동적). 보안(WAS 직접 노출 X), 확장성(독립 스케일링) |
| Stream 모듈은? | TCP/UDP 레이어 프록시. HTTP 외 프로토콜(예: DB 프로토콜) 프록시 가능 |

### REST API 설계

| 질문 | 답변 |
|---|---|
| RESTful 원칙? | 자원 중심 URI, HTTP 메서드 의미 활용, Stateless, HATEOAS(엄격하면) |
| 멱등성(Idempotency)? | 같은 요청 여러 번 해도 결과 동일. GET/PUT/DELETE는 멱등, POST는 보통 아님 |
| 상태 코드? | 200 성공, 201 생성, 204 성공-바디없음, 400 잘못된 요청, 401 인증, 403 권한, 404 없음, 409 충돌, 500 서버 오류 |

### DB 관련

| 질문 | 답변 |
|---|---|
| 인덱스 원리? | B-Tree 기반. 검색 O(log N). 단, 쓰기 비용 ↑, 저장공간 ↑ |
| 트랜잭션 격리 수준? | Read Uncommitted < Read Committed < Repeatable Read < Serializable. PostgreSQL 기본은 RC |
| 무중단 데이터 이관? | dual-write 후 점진적 cutover, 또는 read replica 활용. 핵심은 양쪽 데이터 정합성 검증 |

---

## 🎤 Q&A 시간 (면접관에게 할 질문)

면접 끝에 "질문 있으세요?"는 100% 나옴. **꼭 1~2개 준비.** 안 하면 관심 없어 보임.

### 추천 질문

1. **"디써클 백엔드 팀이 현재 가장 집중하고 있는 기술적 도전은 무엇인가요?"**
   - 회사 현황 파악 + 자기 매칭 가능

2. **"Node.js → Kotlin 마이그레이션은 어떤 방식으로 진행되나요? 점진적 전환인지, 신규 모듈만 Kotlin인지?"**
   - 채용공고 정독 어필 + 실제 업무 환경 파악

3. **"Tech Spec/ADR 문서화 문화가 인상 깊었는데, 어떻게 운영하시는지 궁금합니다."**
   - 채용공고 키워드 직접 인용, 문화 매칭

4. **"입사 후 첫 3개월 동안 기대하시는 기여는 무엇인가요?"**
   - 빠른 적응 의지

### ⛔ 피해야 할 질문
- 연봉, 복지 (1차에선 X)
- 채용공고 보면 알 수 있는 것
- "야근 많아요?" 같은 부정적 톤

---

## 🚨 면접 당일 체크리스트

### 환경 (비대면 면접이라 중요)
- [ ] 카메라 각도 (눈높이, 얼굴 잘 보이게)
- [ ] 조명 (얼굴 어둡지 않게)
- [ ] 마이크 테스트 (이어폰 추천)
- [ ] 인터넷 안정성 (유선 또는 강한 Wi-Fi)
- [ ] 배경 정리 (벽 또는 깔끔한 공간)
- [ ] 면접 5분 전 접속, 1분 전 카메라 ON

### 준비물
- [ ] 이력서 출력 또는 화면 한쪽에 띄우기
- [ ] 이 마크다운 문서 화면 한쪽에 띄우기 (참고용)
- [ ] 물 한 잔
- [ ] 메모지/펜

### 시작 전 마지막 확인
- [ ] 자기소개 한 번 소리 내서 연습
- [ ] 트레이드오프 사례 3개 키워드 떠올리기 (Watson, Logstash, IDP)
- [ ] 회사 정보 한 번 훑기 (rndcircle.io)

### 답변 기본 자세
- [ ] **모르면 모른다고 말하기** — 거짓 답변이 가장 위험
- [ ] **카메라 응시** (모니터 X)
- [ ] **천천히 말하기** — 긴장하면 빨라짐
- [ ] **트레이드오프 의식 멘트 자주** — "이 결정은 X를 얻고 Y를 포기한 것입니다"
- [ ] 답변 끝나면 "여기까지 답변드렸습니다" 같은 명확한 마무리

---

## 🔥 면접 직전 1분 멘탈 리허설

> "나는 4년 4개월간 60억 건 데이터를 다뤄온 백엔드 엔지니어다.
> 운영 안정성과 트레이드오프 판단이 내 강점이다.
> 모르는 건 솔직하게 말하고, 아는 건 자신 있게 말한다.
> 디써클의 비정형 연구 데이터 매칭은 내가 잘하는 영역과 닿아 있다.
> 면접관도 사람이다. 대화하듯 자연스럽게."

---

## 📎 부록: 추가 학습 자료

### 면접 전까지 시간 되면 훑어볼 것
- [Kotlin 공식 문서 - Java to Kotlin 비교](https://kotlinlang.org/docs/comparison-to-java.html)
- [RabbitMQ 공식 튜토리얼 1~3편](https://www.rabbitmq.com/getstarted.html)
- [Spring Batch 공식 문서 - Domain Language](https://docs.spring.io/spring-batch/reference/domain.html)
- [Martin Fowler - 테스트 더블](https://martinfowler.com/bliki/TestDouble.html)

### 회사 직접 체험
- [rndcircle.io 가입해보기](https://www.rndcircle.io/) — 면접 시 한 마디 할 거리

---

**이 문서는 김민수님의 디써클 1차 기술 면접(20분 비대면) 대비용입니다.**
**작성일: 2026년 4월**
